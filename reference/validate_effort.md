# Validator for effort data

Validator for effort data

## Usage

``` r
validate_effort(data, effort_source = c("boat_counting", "fisher_interview"))
```

## Arguments

- data:

  effort data

- effort_source:

  effort_source
